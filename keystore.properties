# Keystore properties for signing the release build
# Place this file at the project root (next to build.gradle).
# Use double backslashes (C:\\path\\to\\file.jks) or forward slashes (C:/path/to/file.jks) on Windows.

# Absolute path to your keystore file
storeFile=G\\:\\Paul\\Visual Studio\\BloodSugarApp\\release-keystore.jks

# Keystore password
storePassword=Em1ly2026!!!!

# Key alias
keyAlias=release_key

# Key password (can be the same as storePassword)
keyPassword=Em1ly2026!!!!

# Explicit keystore type so Gradle reads it correctly (jks or pkcs12)
storeType=pkcs12

# IMPORTANT: Do not commit this file to source control. Add it to .gitignore (already handled if you accepted the .gitignore edit).
